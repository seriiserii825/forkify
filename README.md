# Forkify File API — Monorepo

- **Backend**: Node.js + Express, JSON-файл как БД (совместим с Forkify API).
- **Frontend**: чистая вёрстка + JS, статически сервится live-server.

## Быстрый старт

```bash
cp backend/.env.example backend/.env
npm i
npm run dev
